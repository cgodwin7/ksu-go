<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seniorproject.patrick.ksugo">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CourseContent"></activity>
        <activity android:name=".CourseAnnouncements" />
        <activity android:name=".CourseAssignments" />
        <activity android:name=".CourseDiscussion" />
        <activity android:name=".CourseGrades" />
        <activity android:name=".HomepageStudentTeacher" />
        <activity android:name=".D2L" />
        <activity android:name=".HomepageGuest" />
        <activity android:name=".EmergencyActivity" />
    </application>

</manifest>